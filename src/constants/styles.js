import {
  StyleSheet
} from 'react-native';

export const styles = StyleSheet.create({
  form: {
    borderWidth: 1,
    borderColor: 'black',
    borderRadius: 8,
    paddingVertical: 12,
    paddingHorizontal: 20,
    marginVertical: 16
  },
  label: {
    color: 'black',
    fontSize: 16
  }
});
